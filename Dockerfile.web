FROM node:22-alpine

WORKDIR /app

# Copiar el dist pre-generado
COPY dist ./dist

# Instalar un servidor web simple
RUN npm install -g serve

# Exponer puerto
EXPOSE 3000

# Servir la PWA
CMD ["serve", "-s", "dist", "-l", "3000"]
